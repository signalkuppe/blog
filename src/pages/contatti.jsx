import React from 'react';
import ContattiPage from '../features/contatti/ContattiPage';

export const permalink = '/contatti/index.html';

export default function Contatti({ route }) {
    return <ContattiPage route={route} />;
}
