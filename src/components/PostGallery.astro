---
import { Image, getImage } from "astro:assets";
import PhotoDisclaimer from "./PhotoDisclaimer.astro";
import "photoswipe/dist/photoswipe.css";

interface Props {
  gallery: Array<{
    src: ImageMetadata;
    alt: string;
  }>;
  postSlug: string;
}

const { gallery, postSlug } = Astro.props;
const galleryItems = await Promise.all(
  gallery.map(async (photo) => {
    const lightbox = await getImage({
      src: photo.src,
      width: 2500,
      format: "avif",
      quality: 85,
    });

    const thumbMaxWidth = Math.min(photo.src.width, 640);
    const thumbWidths = [280, 400, 640].filter(
      (width) => width <= thumbMaxWidth,
    );

    return { ...photo, lightbox, thumbMaxWidth, thumbWidths };
  }),
);
const galleryThumbSizes =
  "(max-width: 40rem) 50vw, (max-width: 70rem) 33vw, (max-width: 90rem) 25vw, 280px";
---

<ul id="post-gallery-list" aria-labelledby="post-gallery">
  {
    galleryItems.map((photo) => (
      <li>
        <a
          href={photo.lightbox.src}
          data-pswp-src={photo.lightbox.src}
          data-pswp-width={photo.lightbox.attributes.width}
          data-pswp-height={photo.lightbox.attributes.height}
        >
          <Image
            src={photo.src}
            alt={photo.alt}
            width={photo.thumbMaxWidth}
            widths={photo.thumbWidths}
            sizes={galleryThumbSizes}
            format="webp"
            quality={60}
            loading="lazy"
            decoding="async"
          />
        </a>
      </li>
    ))
  }
</ul>
<p class="disclaimer">
  <PhotoDisclaimer />
</p>

<script>
  //@ts-nocheck
  const lightboxes = new WeakMap();

  async function getLightbox(gallery) {
    let lightboxPromise = lightboxes.get(gallery);
    if (lightboxPromise) return lightboxPromise;

    lightboxPromise = (async () => {
      const [{ default: PhotoSwipeLightbox }, { default: PhotoSwipe }] =
        await Promise.all([
          import("photoswipe/lightbox"),
          import("photoswipe"),
        ]);

      const lightbox = new PhotoSwipeLightbox({
        gallery,
        children: "a",
        pswpModule: PhotoSwipe,
        preloadFirstSlide: false,
        preload: [0, 0],
        showHideAnimationType: "zoom",
        showAnimationDuration: 500,
        hideAnimationDuration: 500,
        bgOpacity: 0.9,
      });
      lightbox.on("uiRegister", function () {
        lightbox.pswp.ui.registerElement({
          name: "custom-caption",
          order: 9,
          isButton: false,
          appendTo: "root",
          html: "Caption text",
          onInit: (el, pswp) => {
            lightbox.pswp.on("change", () => {
              const currSlideElement = lightbox.pswp.currSlide.data.element;
              let captionHTML = "";
              if (currSlideElement) {
                captionHTML = currSlideElement
                  .querySelector("img")
                  .getAttribute("alt");
              }
              el.innerHTML = captionHTML || "";
            });
          },
        });
      });
      lightbox.init();
      return lightbox;
    })();

    lightboxes.set(gallery, lightboxPromise);
    return lightboxPromise;
  }

  function initPostGallery() {
    const gallery = document.getElementById("post-gallery-list");
    if (!gallery || gallery.dataset.lightboxBound === "true") return;

    gallery.dataset.lightboxBound = "true";
    void getLightbox(gallery);
  }

  initPostGallery();
  document.addEventListener("astro:page-load", initPostGallery);
</script>

<style media="screen">
  img {
    display: block;
    width: 100%;
    height: auto;
    background: var(--surfaceAccent);
  }

  .disclaimer {
    font-size: 0.75rem;
    margin-top: 2.5rem;
    max-width: 70ch;
  }

  ul {
    --space: calc(1.5rem - 3px);
    display: block;
    columns: 2;
    column-gap: var(--space);
  }
  li {
    margin-bottom: var(--space);
    break-inside: avoid;
  }

  @media screen and (width > 40rem) {
    ul {
      columns: 3;
    }
  }

  @media screen and (width > 70rem) {
    ul {
      columns: 4;
    }
  }

  :global(.pswp__custom-caption) {
    background: black;
    font-size: 16px;
    color: white;
    width: fit-content;
    padding: 2px 8px;
    border-radius: 4px;
    position: absolute;
    left: 50%;
    bottom: 16px;
    transform: translateX(-50%);
    text-align: center;
  }

  a {
    display: block;
    border: 3px solid transparent;
  }

  a:hover {
    border: 3px solid var(--textAccent);
  }
</style>
